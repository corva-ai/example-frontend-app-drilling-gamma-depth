import { Typography } from '@material-ui/core';

const NoGammaData = () => {
  return (
    <Typography variant="body1">
      The current selected asset has no gamma data available, please select another asset.
    </Typography>
  );
};

export default NoGammaData;
